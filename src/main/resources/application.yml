spring:
  datasource:
    url: jdbc:mysql://mysql_machine/TodoList?useUnicode=yes&characterEncoding=utf8&&useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
server:
  context-path: /api
logging:
  level:
    org.springframework.web: DEBUG